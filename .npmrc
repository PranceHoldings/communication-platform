# pnpm設定（Lambda CDK bundling対策）

# Node modules構造
# Lambda bundling対策: シンボリックリンクを最小化
node-linker=hoisted
shamefully-hoist=true

# キャッシュ設定
store-dir=/tmp/pnpm-cache

# ログレベル
loglevel=warn

# Peer dependencies（初期はfalseで問題回避）
strict-peer-dependencies=false
