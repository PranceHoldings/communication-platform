# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/
.nyc_output/

# Production
build/
dist/
.next/
out/

# Misc
.DS_Store
*.pem

# Environment variables (IMPORTANT: Never commit API keys)
.env
.env.local
.env*.local
infrastructure/.env
infrastructure/.env.backup.*

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Deployment logs
infrastructure/deploy*.log

# Build backups
.next.backup-*
.next.old-*
.next-archive/

# Broken/archived directories (containing files with spaces)
*.broken-*
node_modules.broken*
cdk.out.broken-*
dist.broken-*

# TypeScript build info
*.tsbuildinfo
tsconfig.tsbuildinfo

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code
.claude/

# OS
Thumbs.db

# macOS Finder auto-generated files with spaces (CRITICAL: These break TypeScript compilation)
**/* 2.*
**/*.d 2.ts
**/*  2.js
**/* copy.*

# Turborepo
.turbo/

# AWS CDK
cdk.out/
cdk.out.*/
cdk.out.broken-*
cdk.out.old-*
infrastructure/cdk-outputs.json

# CDK compiled TypeScript outputs (build artifacts - regenerated by tsc)
infrastructure/bin/*.js
infrastructure/bin/*.d.ts
infrastructure/lib/*.js
infrastructure/lib/*.d.ts
infrastructure/scripts/*.js
infrastructure/scripts/*.d.ts

# Lambda build artifacts
infrastructure/lambda/**/deploy/
infrastructure/lambda/**/build.sh

# Temporary files
*.tmp
.cache/
*.backup-*
*.old-*
*.backup
*.backup2
apps/web/.next.broken-*
console.log
consolelog.log
backups/
tobedeleted/

# next-intl migration - DEPRECATED, DO NOT USE
# This directory was removed during migration to custom i18n system
# Keep in .gitignore to prevent accidental re-creation
apps/web/i18n/
**/i18n/request.ts

# Old package.json backups
package.json.old
*.old

# Infrastructure temporary directories
infrastructure/apps/
infrastructure/*.broken-*

# Local backup directories (not part of project)
backup.devcontainer/

# Testing output
test-results/

# Files with spaces (macOS Finder auto-generated copies)
# CRITICAL: Never create files/directories with spaces in names
*\ 2.*
*\ 2/
*\ [0-9].*
*\ [0-9]/
package-lock\ *.json
cdk\ *.out/
*\ *.*
*\ */
*\ *\ *
