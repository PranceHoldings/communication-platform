{
  "name": "@prance/database",
  "version": "0.1.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "echo 'Prisma Client generation is handled at the root level'",
    "generate": "prisma generate"
  },
  "comments": {
    "WARNING": "ローカルPostgreSQLは使用しません。マイグレーションはLambda Migration Function経由でAWS RDSに適用します。"
  },
  "dependencies": {
    "@prisma/client": "5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.9.0",
    "typescript": "^5.3.3"
  }
}
